/** Stile form uniforme ai metadati (label compatta + campi `sm`). */
export const FORM_LABEL = 'mb-0 text-muted small'

/** Spaziatura verticale standard tra gruppi campi */
export const FORM_GROUP_GAP = 'mb-2'

/** Select affiancati (label sopra, controlli allineati in basso sulla stessa riga). */
export const FORM_ADJACENT_ROW = 'd-flex flex-nowrap gap-2 align-items-end'

/**
 * Select compatta: larghezza adattata al testo delle option (non full-width).
 * `mw-100` evita overflow in contenitori stretti.
 */
export const FORM_SELECT = 'w-auto mw-100'

/** Input colore (#hex / rgb / rgba): larghezza minima, cap su stringhe lunghe. */
export const FORM_COLOR_CONTROL = 'font-monospace w-auto mw-100'
